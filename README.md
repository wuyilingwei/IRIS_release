# IRIS Release

These installers are encrypted. Running one requires a valid IRIS license — request access
and manage your credentials at [iris.wuyilingwei.com](https://iris.wuyilingwei.com).

If you meet macOS not allow problem, run `sudo xattr -r -d com.apple.quarantine /Applications/IRIS.app` in terminal.
